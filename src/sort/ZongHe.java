package sort;

/*
 * 插入排序虽然时间复杂度比较高，大样本的时候不行，但在一定范围内（60），小样本的时候，它的常数项很低，所以速度非常快.
 * 在待排序的数组中存放的是基础类型的值，就直接使用快速排序，因为对于电话本这种实例，基本类型整数稳定性没有意义，
 * 但是对于自己定义的类型，为了保证其稳定性，使用归并排序
 */
public class ZongHe {

}
